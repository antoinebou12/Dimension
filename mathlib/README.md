# mathlib

Linear algebra library: dense/sparse matrices, vectors, SVD, 3D math. See the [repository root](../) and [docs](https://docs.rs/mathlib) for more.

For a minimal browser demo (vector add via WebAssembly), see [wasm-demo/](wasm-demo/) and run the build/serve steps there.

## License

MIT
