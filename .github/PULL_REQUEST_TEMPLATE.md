## Description

<!-- What does this PR do? Link any related issue. -->

## Checklist

- [ ] Code lives in **mathlib/** and follows existing patterns.
- [ ] `cargo fmt`, `cargo clippy`, and `cargo test` pass in **mathlib/** (or CI will run them).
- [ ] Docs updated if you changed public APIs or behavior (e.g. `docs/DOCS.md` or doc comments).
- [ ] No known security or correctness regressions; dependency changes are intentional.

## Notes

<!-- Optional: testing done, breaking changes, follow-up work. -->
